# Generated from project template.
# Edit templates/gateway-profiles/hermes/config.yaml, then resync.

platforms:
  api_server:
    enabled: true
    extra:
      host: 0.0.0.0
      port: 8643
      key: "${API_KEY}"

model:
  default: minimax-m2.7
  provider: opencode-go