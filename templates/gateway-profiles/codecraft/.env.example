# Canonical example moved to templates/gateway-profiles.env.example
# Use the [codecraft] section there as the source of truth, then copy the
# relevant variables into templates/gateway-profiles/codecraft/.env.
