# Doubao 网关对外鉴权密钥（调用 :8644 /v1/* 时作为 Bearer token）
API_KEY=

# opencode-go 提供商 API Key（doubao 当前模型调用使用）
OPENCODE_GO_API_KEY=

# 调试开关：是否在 sessions/ 下输出上游请求 dump（排障用）
# true: 开启  false: 关闭
HERMES_DUMP_REQUESTS=true

# 网关授权开关：true 表示允许所有用户访问（仅建议本地/内网测试）
GATEWAY_ALLOW_ALL_USERS=
