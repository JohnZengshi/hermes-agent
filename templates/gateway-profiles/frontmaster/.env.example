# Canonical example moved to templates/gateway-profiles.env.example
# Use the [frontmaster] section there as the source of truth, then copy the
# relevant variables into templates/gateway-profiles/frontmaster/.env.
